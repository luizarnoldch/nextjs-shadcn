import FormTestView from "@/features/form-test/views/FormTestView";

export const metadata = {
  title: "Form Test Lab",
  description: "A page to test all data types in a form.",
};

export default function FormTestPage() {
  return <FormTestView />;
}
